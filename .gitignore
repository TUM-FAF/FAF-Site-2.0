/.idea
node_modules/
dist/
.env

# Dev/test workspace — never commit
bk/

# Generated game build output — rebuilt from source
public/games/

# Platform-specific lockfile causes native binding failures on Linux CI
package-lock.json