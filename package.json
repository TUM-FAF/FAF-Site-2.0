{
  "name": "new_site",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "build:game": "npm --prefix games/space-invaders install && npm --prefix games/space-invaders run build"
  },
  "dependencies": {
    "astro": "^6.0.8"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.2.2"
  }
}
